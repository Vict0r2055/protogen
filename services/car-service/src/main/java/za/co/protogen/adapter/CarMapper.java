package za.co.protogen.adapter;

import org.mapstruct.Mapper;
// import org.mapstruct.Mapping;
// import za.co.protogen.controller.models.CarDTO;
// import za.co.protogen.domain.models.CarDomain;
// import za.co.protogen.persistence.models.CarEntity;
// import za.co.protogen.proxy.models.CarProxy;

// @Mapper(componentModel = "spring")
public interface CarMapper {

    // @Mapping(target = "carId", ignore = true)
    // CarDomain controllerToDomain(CarDTO controllerDTO);

    // CarEntity domainToEntity(CarDomain domainModel);

    // CarDomain entityToDomain(CarEntity entityModel);

    // CarProxy domainToProxy(CarDomain domainModel);

    // Add other mappings as needed
}
